version: '3.8'

services:
  bot:
    build: .
    ports:
      - "8000:8000"
    environment:
      - ENVIRONMENT=production
    volumes:
      - bot_data:/app/data
    restart: unless-stopped

volumes:
  bot_data:
